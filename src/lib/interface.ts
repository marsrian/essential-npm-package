export interface BlogPost {
    title: string;
    currentSlug: string;
    image: string;
}

export interface FullBlog {
    title: string;
    currentSlug: string;
    image: string;
    body: any;
}
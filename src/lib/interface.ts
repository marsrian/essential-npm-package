export interface BlogPost {
    title: string;
    currentSlug: string;
    image: string;
}

export interface FullBlog {
    currentSlug: string;
    title: string;
    body: any;
    image: string;
}